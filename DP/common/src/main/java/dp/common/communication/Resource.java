package dp.common.communication;

/**
 * 
 * Modelize the "model" class
 * @author David Sene && Pierre Rainero
 *
 */
public enum Resource {
	IDEA(),
	REPOSITORY(),
	STUDENT(),
	CONNECTION(),
	CLOSE();
}
